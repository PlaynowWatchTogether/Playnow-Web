<div class="who">Who:</div>
{{#if hasChips}}

  <div class="chips-holder">
    <div class="chips-wrapper">

      {{#each chips as |chip|}}
        {{#compose-chip model=chip classNames="chip" onChipClick=(action "onChipClick")}}

        {{/compose-chip}}
      {{/each}}
    </div>
  </div>
{{/if}}
{{#custom-typeahead
  id="typeahead-compose"
  content=friends
  displayKey="firstName"
  valueToken="firstName"
  placeholder="Type the username of the person"
  class="form-control"
  selectOnEnter=true
  suggestionTemplate=suggestionTemplate
  emptyTemplate=emptyTemplate
  limit=numberofresultstodisplay
  onSelectAction=(action "selectFriend")}}

{{/custom-typeahead}}
{{yield}}