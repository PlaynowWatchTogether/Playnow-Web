{{#if display}}
	{{#user-profile-view model=model.id}}
	  <div class="profile-back" style={{myStyle}}></div>

	  <div class="profilePicWrapper" style={{myStyle}}>
	    <img src={{profilePic}} class="profilePic" alt="icon">
	  </div>
	{{/user-profile-view}}  
{{/if}}